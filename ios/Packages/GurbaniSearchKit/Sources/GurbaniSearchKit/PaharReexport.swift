// Pahar lives in its own dependency-free product (GurbaniPahar) so app extensions can link
// it without CSQLite; re-exported here so `import GurbaniSearchKit` keeps seeing Pahar.
@_exported import GurbaniPahar
