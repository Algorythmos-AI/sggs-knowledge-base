// Poster 09 — the five bounded contexts, module slicing, and the gateway generated from the code.
const V = { version: '1.3.9', date: '2026-09-25', commit: '42ed0338' };
export default {
  number: '09', slug: 'bounded-contexts-and-gateway',
  title: 'Bounded contexts and the gateway',
  subtitle: 'One codebase, five contexts: how a service runs a slice of it, and how routing is generated so it cannot drift',
  description: 'The 26 routes are tagged with five bounded contexts (reader, search, verify, insights, knowledge), each declaring the tables it reads. SGGS_MODULES selects the contexts a process serves and readiness refuses a database missing any declared table. gateway/routes.json says per environment where /api goes; tools/gen_gateway.py generates the Vercel rewrites from the route table, and CI proves the routing with X-Service probes and the whole golden contract.',
  height: 1080, verified: V,
  sources: ['webapp/serve.py', 'gateway/routes.json', 'tools/gen_gateway.py', 'tools/build_api_functions.py', 'docs/adr/0010-services-behind-a-generated-gateway.md', 'docs/adr/0011-api-as-functions-in-the-web-project.md'],
  legend: ['box', 'store', 'pin', 'gate', 'good', 'note'],
  groups: [
    { label: 'webapp/sggs — five bounded contexts, one image', x: 40, y: 150, w: 760, h: 470 },
    { label: 'Routing, generated from the code', x: 840, y: 150, w: 720, h: 470 },
    { label: 'Proven on every staging deploy', x: 40, y: 680, w: 1520, h: 240 },
  ],
  nodes: [
    { id: 'reader', kind: 'box', x: 70, y: 200, w: 340, h: 92, lines: ['reader', { text: 'meta · ang · shabad · health', size: 16 }, { text: 'random · lines · banis · bani', size: 16 }], step: 'step-01' },
    { id: 'search', kind: 'box', x: 70, y: 310, w: 340, h: 76, lines: ['search', { text: 'search · word', size: 16 }], step: 'step-01' },
    { id: 'verify', kind: 'box', x: 70, y: 404, w: 340, h: 76, lines: ['verify', { text: 'verify', size: 16 }], step: 'step-01' },
    { id: 'insights', kind: 'box', x: 430, y: 200, w: 340, h: 92, lines: ['insights', { text: 'themes · analytics/* · related', size: 16 }, { text: 'line_concepts · neighbors', size: 16 }], step: 'step-01' },
    { id: 'knowledge', kind: 'box', x: 430, y: 310, w: 340, h: 92, lines: ['knowledge', { text: 'timing/clock · timing/raag', size: 16 }, { text: 'timing/divergence · forms', size: 16 }], step: 'step-01' },
    { id: 'tables', kind: 'store', x: 430, y: 420, w: 340, h: 76, lines: ['TABLES per context', { text: 'CONTEXT_TABLES → DB slices', size: 16 }], step: 'step-02' },
    { id: 'modules', kind: 'note', x: 70, y: 520, w: 700, h: 76, lines: [{ text: 'SGGS_MODULES=all | reader,search,… → ENABLED', mono: true, size: 16 }, { text: 'unknown name = error · route outside ENABLED = 404 · readiness = tables', size: 16 }], step: 'step-02' },
    { id: 'routes', kind: 'store', x: 870, y: 200, w: 320, h: 92, lines: ['serve.ROUTES', { text: '(seg1, seg2) →', size: 16, mono: true }, { text: '(handler, context)', size: 16, mono: true }], step: 'step-03' },
    { id: 'cfg', kind: 'pin', x: 1220, y: 200, w: 310, h: 92, lines: [{ text: 'gateway/routes.json', mono: true }, { text: 'staging: vercel, 5 services', size: 16 }, { text: 'production: render', size: 16 }], step: 'step-03' },
    { id: 'gen', kind: 'box', x: 870, y: 320, w: 660, h: 92, lines: ['tools/gen_gateway.py', { text: 'every prefix of a routed context (+ its /api/v1 twin) → its function', size: 16 }, { text: 'everything else → all', size: 16 }], step: 'step-03' },
    { id: 'vercel', kind: 'store', x: 870, y: 460, w: 320, h: 120, lines: ['frontend/vercel.json', { text: 'staging (host-conditioned):', size: 16 }, { text: '/api/ang(/.*)? → svc/reader', size: 16, mono: true }, { text: 'production: /api → Render', size: 16 }], step: 'step-04' },
    { id: 'fns', kind: 'box', x: 1220, y: 460, w: 310, h: 120, lines: ['API functions (staging)', { text: 'api/svc/<ctx>.py + all', size: 16, mono: true }, { text: 'fix SGGS_MODULES, DB, commit', size: 16 }, { text: 'built at deploy, not committed', size: 16 }], step: 'step-04' },
    { id: 'p1', kind: 'gate', x: 70, y: 730, w: 340, h: 76, lines: ['gen_gateway.py --check', { text: 'vercel.json = generated', size: 16 }], step: 'step-05' },
    { id: 'p2', kind: 'gate', x: 430, y: 730, w: 340, h: 76, lines: ['--verify-output', { text: 'a function bundles only its slice', size: 16 }], step: 'step-05' },
    { id: 'p3', kind: 'gate', x: 790, y: 730, w: 360, h: 76, lines: ['X-Service probes', { text: '/api/ang/1 → reader · /api/nope → all', size: 16 }], step: 'step-05' },
    { id: 'p4', kind: 'good', x: 1170, y: 730, w: 360, h: 76, lines: ['Golden contract via the gateway', { text: '266 records on staging', size: 16 }], step: 'step-05' },
    { id: 'rollback', kind: 'note', x: 70, y: 840, w: 1460, h: 60, lines: [{ text: 'Reversible in one line: remove a context from gateway/routes.json and the single API answers it again (ADR-0010 / ADR-0011)', size: 16 }], step: 'step-06' },
  ],
  edges: [
    { from: 'reader', to: 'tables', dashed: true, step: 'step-02' }, { from: 'search', to: 'tables', dashed: true, step: 'step-02' }, { from: 'verify', to: 'tables', dashed: true, step: 'step-02' },
    { from: 'insights', to: 'tables', dashed: true, step: 'step-02' }, { from: 'knowledge', to: 'tables', dashed: true, step: 'step-02' },
    { from: 'routes', to: 'gen', step: 'step-03' }, { from: 'cfg', to: 'gen', step: 'step-03' },
    { from: 'gen', to: 'vercel', label: 'writes', step: 'step-04' }, { from: 'gen', to: 'fns', label: 'functions()', step: 'step-04' },
    { from: 'tables', to: 'fns', dashed: true, label: 'slices', step: 'step-04', via: [[820, 458], [820, 645], [1375, 645]], dy: -14 },
    { from: 'vercel', to: 'p1', dashed: true, step: 'step-05', via: [[1030, 632], [240, 632]] }, { from: 'fns', to: 'p2', dashed: true, step: 'step-05', via: [[1420, 600], [1420, 662], [600, 662]] },
    { from: 'p3', to: 'p4', step: 'step-05' },
  ],
  steps: [
    { id: 'step-01', title: 'Five contexts, 26 routes', caption: 'Every route in serve.ROUTES is tagged with the bounded context that owns it: reader (pages, compositions, health, banis), search, verify, insights (themes, analytics, neighbours) and knowledge (raag timing, forms). No two contexts share a path prefix, so any route can be sent to exactly one service.', link: '/architecture/overview/' },
    { id: 'step-02', title: 'Each context declares its tables', caption: 'Each module lists the tables it reads (CONTEXT_TABLES). SGGS_MODULES chooses the contexts a process serves — all by default, or a comma list to run one service from the same image; an unknown name is an error, a route outside the enabled set is a 404, and readiness refuses a database missing any declared table. The same lists cut the per-service database slices.', link: '/adr/0010-services-behind-a-generated-gateway/' },
    { id: 'step-03', title: 'Routing is generated, never hand-written', caption: 'gateway/routes.json says, per environment, where /api goes: on staging to Vercel functions, one per context plus all; on production to all alone, until contexts are split out there one release at a time. tools/gen_gateway.py combines it with the route table to produce the rewrites — every prefix of a routed context and its /api/v1 twin, then a catch-all to all.', link: '/adr/0011-api-as-functions-in-the-web-project/' },
    { id: 'step-04', title: 'What ships', caption: 'The committed frontend/vercel.json carries only the rewrites (host-conditioned for staging; production sends /api to all). At deploy time tools/build_api_functions.py generates the functions — each fixing its SGGS_MODULES, its database slice and the commit — and adds their configuration; nothing generated is committed.', link: '/process/runbooks/deploy/' },
    { id: 'step-05', title: 'Proven on every deploy', caption: 'CI fails if vercel.json is not what the code generates, if a function bundles anything but its own code and slice, if any context answers with the wrong X-Service (or an unknown prefix does not fall through to all), or if the 266-record golden contract does not pass through the gateway on the staging site.', link: '/process/ci-gates/' },
    { id: 'step-06', title: 'And reversible in one line', caption: 'Removing a context from gateway/routes.json sends its routes back to the single API on the next deploy. Production answers /api with all and splits contexts out one release at a time, gated on the performance baseline and the data canary; setting api_platform back to render returns it to the Render single API, which keeps deploying as the rollback.', link: '/process/runbooks/services-production/' },
  ],
};
