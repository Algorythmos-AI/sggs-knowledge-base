// Poster 11 — from a branch to production and the App Store, the way the pipeline actually runs it.
const V = { version: '1.3.9', date: '2026-09-25', commit: 'f25ab97f' };
export default {
  number: '11', slug: 'delivery-pipeline',
  title: 'The delivery pipeline',
  subtitle: 'From a branch to staging, to a release on main, to production and the App Store — every step gated by CI, verified by commit, and reversible',
  description: 'Work branches from integration, passes the local gates and a pull request’s checks, and is squash-merged; the push runs deploy-staging, which builds the API functions and the web at that commit, aliases the staging site and verifies it. A release is a merge-commit pull request from integration to main; deploy-production waits for every required check plus playwright on the exact commit, deploys the API and verifies it by commit, builds the web unaliased and smokes it, promotes it with a public smoke and a rollback edge, then tags vX.Y.Z. The app re-archives at the tag so web, API and App Store share one version. Afterwards the data canary and uptime keep watching.',
  height: 1200, verified: V,
  sources: ['.github/workflows/deploy-production.yml', '.github/workflows/deploy-staging.yml', 'docs/process/branching.md', 'docs/process/release.md'],
  legend: ['actor', 'box', 'gate', 'good', 'ext', 'note'],
  groups: [
    { label: 'Ship — into integration, onto staging', x: 40, y: 140, w: 1520, h: 300 },
    { label: 'Release — into main, onto production (deploy-production.yml)', x: 40, y: 470, w: 1520, h: 450 },
  ],
  nodes: [
    { id: 'dev', kind: 'actor', x: 60, y: 190, w: 260, h: 80, lines: ['Engineer', { text: 'feat/… from integration', size: 16 }], step: 'step-01' },
    { id: 'local', kind: 'box', x: 350, y: 190, w: 280, h: 80, lines: ['Local gates', { text: 'make ci · make docs-check', size: 16 }], step: 'step-01' },
    { id: 'pr', kind: 'gate', x: 660, y: 190, w: 280, h: 80, lines: ['PR → integration', { text: 'every check on the commit', size: 16 }], step: 'step-02' },
    { id: 'merge', kind: 'box', x: 970, y: 190, w: 260, h: 80, lines: ['Squash merge', { text: 'owner: gh pr merge --admin', size: 16 }], step: 'step-03' },
    { id: 'staging', kind: 'good', x: 1260, y: 190, w: 280, h: 80, lines: ['deploy-staging', { text: 'gates · preflight · build', size: 16 }], step: 'step-04' },
    { id: 'sverify', kind: 'box', x: 660, y: 320, w: 880, h: 90, lines: [{ text: 'verify: functions ready at the commit · gateway X-Service · web smoke · the contract', size: 18, weight: 600 }, { text: 'then a person reviews sggs-staging.vercel.app (SSO) before any release', size: 16 }], step: 'step-05' },
    { id: 'rpr', kind: 'gate', x: 60, y: 520, w: 300, h: 100, lines: ['Release PR → main', { text: 'make release VERSION=X.Y.Z', size: 16 }, { text: 'CHANGELOG section · merge commit', size: 16 }], step: 'step-06' },
    { id: 'gates', kind: 'gate', x: 390, y: 520, w: 300, h: 100, lines: ['gates + preflight', { text: 'required checks + playwright', size: 16 }, { text: 'every production secret present', size: 16 }], step: 'step-07' },
    { id: 'approve', kind: 'box', x: 720, y: 520, w: 260, h: 100, lines: ['approve', { text: 'optional human click', size: 16 }, { text: '(production-approval env)', size: 16 }], step: 'step-07' },
    { id: 'api', kind: 'ext', x: 1010, y: 520, w: 260, h: 100, lines: ['deploy-api → verify-api', { text: 'Render hook ref=SHA;', size: 16 }, { text: 'health.commit == SHA', size: 16 }], step: 'step-08' },
    { id: 'web', kind: 'box', x: 1300, y: 520, w: 240, h: 100, lines: ['web: deploy · verify', { text: 'vercel build, unaliased', size: 16 }, { text: '@smoke before it is live', size: 16 }], step: 'step-09' },
    { id: 'promote', kind: 'good', x: 60, y: 660, w: 620, h: 90, lines: ['promote', { text: 'vercel promote · CDN purged · @smoke on gurbanisoul.com', size: 16 }, { text: 'smoke fails → automatic vercel rollback + incident issue', size: 16 }], step: 'step-10' },
    { id: 'tag', kind: 'good', x: 720, y: 660, w: 400, h: 90, lines: ['release: tag vX.Y.Z', { text: 'GitHub Release from the CHANGELOG', size: 16 }, { text: 'only after a verified deploy', size: 16 }], step: 'step-11' },
    { id: 'ios', kind: 'ext', x: 1150, y: 660, w: 390, h: 90, lines: ['gurbani-soul-ios re-archives', { text: 'vendor-sync at vX.Y.Z · X.Y.Z (1)', size: 16 }, { text: 'check_release_complete.py', size: 16 }], step: 'step-11' },
    { id: 'rollback', kind: 'note', x: 60, y: 790, w: 620, h: 100, lines: ['Roll back', { text: 'web: vercel rollback (automatic on a failed smoke)', size: 16 }, { text: 'API: Render previous deploy · data: revert the lock bump', size: 16 }], step: 'step-10' },
    { id: 'watch', kind: 'note', x: 720, y: 790, w: 820, h: 100, lines: ['After the release', { text: 'data canary every 6 h: served scripture == the pin, origin and CDN, contract replayed', size: 16 }, { text: 'uptime every 15 min · make verify-prod proves commit, health, Ang 712, deployment', size: 16 }], step: 'step-12' },
    { id: 'docsnote', kind: 'note', x: 40, y: 960, w: 1520, h: 70, lines: [{ text: 'The wiki follows the same chain in deploy-docs.yml: integration → the docs staging alias; main → deploy unaliased, smoke by commit, promote, smoke, roll back', size: 16 }], step: 'step-12' },
  ],
  edges: [
    { from: 'dev', to: 'local', step: 'step-01' }, { from: 'local', to: 'pr', step: 'step-02' }, { from: 'pr', to: 'merge', step: 'step-03' }, { from: 'merge', to: 'staging', step: 'step-04' },
    { from: 'staging', to: 'sverify', step: 'step-05' },
    { from: 'sverify', to: 'rpr', dashed: true, step: 'step-06', via: [[1100, 455], [48, 455], [48, 570]] },
    { from: 'rpr', to: 'gates', step: 'step-07' }, { from: 'gates', to: 'approve', step: 'step-07' }, { from: 'approve', to: 'api', step: 'step-08' }, { from: 'api', to: 'web', step: 'step-09' },
    { from: 'web', to: 'promote', step: 'step-10', via: [[1420, 640], [370, 640]] },
    { from: 'promote', to: 'tag', step: 'step-11' }, { from: 'tag', to: 'ios', step: 'step-11' },
    { from: 'promote', to: 'rollback', dashed: true, step: 'step-10' }, { from: 'tag', to: 'watch', dashed: true, step: 'step-12' },
  ],
  steps: [
    { id: 'step-01', title: 'Branch and run the local gates', caption: 'Branch feat/… or fix/… from integration, the trunk. Before opening a pull request run make ci (versions, dataset check, the server tests, the contract, the docs gates) so the pull request’s checks are a confirmation, not a discovery.', link: '/onboarding/first-pr/' },
    { id: 'step-02', title: 'The pull request', caption: 'Every workflow on the CI gates map runs on the commit: web-ci, scripture-integrity, version-consistency, security, pr-hygiene, e2e and the docs job. A change to scripture handling, search, verification or the dataset pin completes the scripture-safety checklist in the template.', link: '/process/ci-gates/' },
    { id: 'step-03', title: 'Squash merge', caption: 'Feature and fix pull requests are squash-merged into integration so the trunk carries one clean commit per change. Only the owner merges (the ruleset requires CODEOWNERS); the engineer hands over the exact command.', link: '/process/branching/' },
    { id: 'step-04', title: 'deploy-staging', caption: 'The push runs deploy-staging: the gates job waits for the core checks and playwright on the commit, preflight checks the staging secrets, the API functions are generated from the pinned database (each context’s slice proven by the slicer), vercel build proves what each function bundles, the deployment is aliased to sggs-staging.vercel.app and the CDN cache is purged.', link: '/process/runbooks/deploy/' },
    { id: 'step-05', title: 'Verify staging, then a person looks', caption: 'verify_functions.py checks every function’s /readyz reports this commit and exactly its contexts; the gateway probes prove each context answers through the real rewrites with its own X-Service; the web smoke runs; the whole golden contract passes through the gateway. Then the owner reviews the staging site, logged in to Vercel, before any release.', link: '/process/environments/' },
    { id: 'step-06', title: 'The release pull request', caption: 'make release VERSION=X.Y.Z bumps the unified version everywhere and stubs the CHANGELOG section; a dataset change arrived earlier as a reviewed lock bump with a regenerated contract. The pull request goes from integration to main and is merged as a merge commit — never squashed — so main stays a descendant of integration.', link: '/process/release/' },
    { id: 'step-07', title: 'gates, preflight, approve', caption: 'deploy-production waits until every check the main ruleset requires, plus playwright, has succeeded on the exact merge commit; preflight proves every production secret exists; approve is an optional human click when the production-approval environment has reviewers. Nothing has been deployed yet.', link: '/process/runbooks/deploy/' },
    { id: 'step-08', title: 'The API, verified by commit', caption: 'deploy-api triggers the Render deploy hook with ref=SHA and waits until /api/health.commit equals the commit and the health checks are all true. verify-api then samples compositions and proves exact lines, present headings and 404 on the permanent comp_id gaps. The web is untouched until this passes.', link: '/process/runbooks/deploy/' },
    { id: 'step-09', title: 'The web, built unaliased and smoked', caption: 'vercel build and deploy with --skip-domain produce a deployment nobody can reach yet; the @smoke tests run against it. A failure here leaves users on the previous deployment.', link: '/process/runbooks/deploy/' },
    { id: 'step-10', title: 'Promote — with a way back', caption: 'vercel promote puts the deployment on gurbanisoul.com, the CDN cache is purged so no stale API body outlives its dataset, and the @smoke tests run against the public domain. If they fail, the job runs vercel rollback to the recorded previous deployment and opens an incident issue. The API rolls back from Render’s previous deploy; a dataset rolls back by reverting the lock bump.', link: '/process/runbooks/rollback/' },
    { id: 'step-11', title: 'Tag, and the app follows', caption: 'Only after a verified deploy does the release job create the tag vX.Y.Z and a GitHub Release from the CHANGELOG section. The app repository then vendors the contract at that tag, sets MARKETING_VERSION and uploads X.Y.Z (1) with CHANNEL=appstore; scripts/release/check_release_complete.py proves web, API and the binary report the same number from the same commit.', link: '/engineering/delivery/' },
    { id: 'step-12', title: 'After the release', caption: 'make verify-prod proves what production serves by commit identity. Every six hours the data canary proves the served scripture equals the pin at the origin and through the CDN and replays the golden contract; every fifteen minutes uptime probes /api/health and the App Store URLs. The wiki deploys through the same chain in deploy-docs.yml.', link: '/process/ci-gates/' },
  ],
};
