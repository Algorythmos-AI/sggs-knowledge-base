// Poster 08 — one request, from the browser to SQLite and back (webapp/serve.py H._handle).
const V = { version: '1.3.9', date: '2026-09-25', commit: '42ed0338' };
export default {
  number: '08', slug: 'request-lifecycle',
  title: 'Request lifecycle',
  subtitle: 'What happens to GET /api/ang/712 between the browser and the database — in the order the code does it',
  description: 'The browser calls /api same-origin; the host rewrites it to the API; H._handle assigns a request id, answers liveness and readiness first, then routes /api/v1 or /api through the api() dispatcher and the ROUTES table into the bounded context that owns the route; the context reads the read-only, immutable SQLite database; _respond adds caching, an ETag, the security headers, X-Request-Id and X-Service; the access log records the path only. Errors map to 404, 400 or a generic 500.',
  height: 1060, verified: V,
  sources: ['webapp/serve.py', 'webapp/sggs/core.py', 'gateway/routes.json', 'tools/gen_gateway.py'],
  legend: ['actor', 'box', 'ext', 'store', 'gate', 'note'],
  nodes: [
    { id: 'browser', kind: 'actor', x: 40, y: 170, w: 280, h: 70, lines: ['Browser / app', { text: 'GET /api/ang/712', size: 16, mono: true }], step: 'step-01' },
    { id: 'edge', kind: 'ext', x: 400, y: 160, w: 360, h: 90, lines: ['Host: Vercel rewrite', { text: 'staging: → function api/svc/reader', size: 16 }, { text: 'production: → Render single API', size: 16 }], step: 'step-01' },
    { id: 'rid', kind: 'box', x: 840, y: 160, w: 340, h: 100, lines: ['H._handle · request id', { text: 'reuse X-Request-Id / x-vercel-id', size: 16 }, { text: 'if a plain token, else uuid4', size: 16 }], step: 'step-02' },
    { id: 'probe', kind: 'box', x: 1260, y: 160, w: 300, h: 100, lines: ['/healthz · /readyz', { text: 'liveness · readiness =', size: 16 }, { text: 'every declared table present', size: 16 }], step: 'step-02' },
    { id: 'v1', kind: 'box', x: 840, y: 300, w: 340, h: 90, lines: ['/api/v1/* → _handle_v1', { text: 'same bodies; unknown → 404;', size: 16 }, { text: 'errors {code, message, request_id}', size: 16 }], step: 'step-03' },
    { id: 'legacy', kind: 'box', x: 1260, y: 300, w: 300, h: 90, lines: ['/api/* (legacy, pinned)', { text: 'unknown → 400; else static', size: 16 }, { text: 'files, realpath-jailed', size: 16 }], step: 'step-03' },
    { id: 'api', kind: 'box', x: 840, y: 430, w: 340, h: 100, lines: ['api() dispatcher', { text: 'ROUTES[(seg1, seg2)]', size: 16, mono: true }, { text: '26 routes · context ∉ ENABLED → 404', size: 16 }], step: 'step-04' },
    { id: 'ctx', kind: 'box', x: 840, y: 580, w: 340, h: 100, lines: ['Bounded context module', { text: 'sggs/reader.py · _route_ang', size: 16, mono: true }, { text: 'parameterised SQL · _int()', size: 16 }], step: 'step-05' },
    { id: 'db', kind: 'store', x: 840, y: 740, w: 340, h: 90, lines: ['SQLite', { text: 'mode=ro&immutable=1', size: 16, mono: true }, { text: 'one connection per handler thread', size: 16 }], step: 'step-05' },
    { id: 'respond', kind: 'box', x: 400, y: 430, w: 360, h: 120, lines: ['_respond', { text: 'cacheable: public, max-age=300,', size: 16 }, { text: 's-maxage=3600 + ETag → 304', size: 16 }, { text: 'else no-store', size: 16 }], step: 'step-06' },
    { id: 'sec', kind: 'box', x: 400, y: 600, w: 360, h: 120, lines: ['_sec_headers', { text: 'nosniff · X-Frame-Options DENY', size: 16 }, { text: 'no-referrer · HSTS', size: 16 }, { text: 'X-Request-Id · X-Service', size: 16 }], step: 'step-06' },
    { id: 'log', kind: 'note', x: 400, y: 770, w: 360, h: 80, lines: ['Access log (stderr, JSON)', { text: 'method, path, status, ms, id', size: 16 }, { text: 'never the query string', size: 16 }], step: 'step-07' },
    { id: 'err', kind: 'gate', x: 1260, y: 430, w: 300, h: 120, lines: ['Errors', { text: 'ApiError → its status (404)', size: 16 }, { text: 'ValueError → 400', size: 16 }, { text: 'else → 500, generic body', size: 16 }], step: 'step-08' },
    { id: 'pool', kind: 'note', x: 1260, y: 600, w: 300, h: 100, lines: ['Bounded server', { text: 'SGGS_MAX_WORKERS (48)', size: 16 }, { text: 'socket timeout 15 s', size: 16 }, { text: 'idle clients drop', size: 16 }], step: 'step-08' },
    { id: 'out', kind: 'actor', x: 40, y: 460, w: 280, h: 70, lines: ['Browser / app', { text: 'verbatim lines · Ang 712', size: 16 }], step: 'step-07' },
  ],
  edges: [
    { from: 'browser', to: 'edge', step: 'step-01' }, { from: 'edge', to: 'rid', step: 'step-02' },
    { from: 'rid', to: 'probe', label: 'first', step: 'step-02', dy: -16 },
    { from: 'rid', to: 'v1', label: 'then', step: 'step-03' }, { from: 'probe', to: 'legacy', step: 'step-03' },
    { from: 'v1', to: 'api', step: 'step-04' }, { from: 'legacy', to: 'api', step: 'step-04', via: [[1220, 400]] },
    { from: 'api', to: 'ctx', label: 'route[0](p, qs)', step: 'step-05', dx: 110 }, { from: 'ctx', to: 'db', label: 'SELECT … WHERE ang=?', step: 'step-05' },
    { from: 'ctx', to: 'respond', label: 'JSON body', step: 'step-06', via: [[800, 630], [800, 490]], dx: 36, dy: 40 }, { from: 'respond', to: 'sec', step: 'step-06' },
    { from: 'sec', to: 'log', step: 'step-07' }, { from: 'sec', to: 'out', label: '200 · 304', step: 'step-07', via: [[360, 660], [360, 495]] },
    { from: 'api', to: 'err', dashed: true, step: 'step-08' }, { from: 'err', to: 'respond', dashed: true, step: 'step-08', via: [[1410, 572], [800, 572]] },
  ],
  steps: [
    { id: 'step-01', title: 'Same-origin call', caption: 'The browser (or a wiki widget) calls /api/ang/712 on the site’s own host. There is no CORS and no API base URL in the frontend: a Vercel rewrite forwards the request — on staging to the context’s own function, on production to the single Render API. The rules are generated from the route table.', link: '/architecture/bounded-contexts-and-gateway/' },
    { id: 'step-02', title: 'Identity and probes first', caption: 'H._handle stamps the clock and a request id — an incoming X-Request-Id or x-vercel-id is reused only if it is a plain token, otherwise a uuid4 — then answers /healthz (liveness) and /readyz (every table each enabled context declares is present) before anything else.', link: '/adr/0009-production-verification/' },
    { id: 'step-03', title: 'Two API surfaces, one body', caption: '/api/v1/* has strict semantics: an unknown endpoint is 404 and every error is {code, message, request_id}. /api/* is the legacy surface whose responses are byte-pinned by the golden contract. Anything else is a static file, served from a realpath jail.', link: '/api/reference/' },
    { id: 'step-04', title: 'The dispatcher', caption: 'api() splits the path, ensures FTS5 is detected, and looks up (first segment, second segment) in ROUTES — 26 routes, each tagged with its bounded context. In split mode a route whose context is not enabled in this process is a 404: a service never answers for another.', link: '/architecture/bounded-contexts-and-gateway/' },
    { id: 'step-05', title: 'The context reads SQLite', caption: 'The handler in sggs/reader.py runs parameterised SQL (never string-built; FTS columns allowlisted; every integer through _int()) on a connection opened read-only and immutable, one per handler thread. The app can never write to the scripture.', link: '/engineering/invariants/' },
    { id: 'step-06', title: 'Respond: caching and headers', caption: 'Responses that depend only on the immutable database (ang, shabad, lines, analytics, timing …) are cacheable for the browser and the CDN, with a sha256 ETag that turns a repeat into a 304. Every response carries nosniff, X-Frame-Options DENY, no-referrer, HSTS, the request id and X-Service (which service answered).', link: '/engineering/invariants/' },
    { id: 'step-07', title: 'Log the path, never the query', caption: 'One JSON line per request goes to stderr: method, path, status, milliseconds and the request id. The query string is never written, so what people search for is not recorded anywhere. The verbatim lines of Ang 712 reach the browser.', link: '/adr/0009-production-verification/' },
    { id: 'step-08', title: 'Errors and limits', caption: 'An ApiError keeps its status (a missing composition is 404); a bad parameter is 400 with a short message; anything unexpected is a generic 500 — the detail goes to the server log, never over the wire. The server caps handler threads and times out idle sockets so a slow client cannot hold a slot.', link: '/engineering/invariants/' },
  ],
};
