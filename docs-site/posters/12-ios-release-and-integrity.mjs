// Poster 12 — how a platform release becomes an App Store build, and the two ladders that run on the device.
const V = { version: '1.3.9', date: '2026-09-25', commit: '417c92f6' };
export default {
  number: '12', slug: 'ios-release-and-integrity',
  title: 'iOS release and integrity',
  subtitle: 'From the platform tag to an App Store binary that carries the same number — every step a gate — and what the app proves to itself before it shows a single line',
  description: 'A platform release is vendored into the app repository at its tag with a sha256 per file; the app’s MARKETING_VERSION must equal that release. make testflight, run from the tag, stages the database for the chosen profile, runs the licence gate on that artifact, generates a temporary Xcode project, archives and uploads, proves the Info.plist and the manifest inside the app, and records the build in the ledger; check_release_complete.py then proves web, API and binary share one version and one commit. On the device, LaunchIntegrity hashes the bundled database against its manifest and refuses to show scripture on a mismatch; bookmarks live in a versioned SwiftData store that is only recreated after a second consecutive failure.',
  height: 1120, verified: V,
  sources: ['gurbani-soul-ios/ios/tools/testflight_archive.sh', 'gurbani-soul-ios/ios/App/Sources/Data/LaunchIntegrity.swift', 'gurbani-soul-ios/scripts/vendor_sync.py', 'scripts/release/check_release_complete.py'],
  legend: ['pin', 'box', 'gate', 'good', 'store', 'ext', 'note'],
  groups: [
    { label: 'Release — from the platform tag to the ledger (make testflight, from a detached worktree at the tag)', x: 40, y: 140, w: 1520, h: 470 },
    { label: 'On the device — launch integrity', x: 40, y: 640, w: 760, h: 340 },
    { label: 'On the device — bookmarks', x: 830, y: 640, w: 730, h: 340 },
  ],
  nodes: [
    { id: 'tag', kind: 'ext', x: 60, y: 190, w: 280, h: 90, lines: ['Platform tag vX.Y.Z', { text: 'contract/ · contributors.json', size: 16 }, { text: 'APP_VERSION', size: 16, mono: true }], step: 'step-01' },
    { id: 'vendor', kind: 'pin', x: 370, y: 190, w: 300, h: 90, lines: [{ text: 'vendor.lock.json', mono: true }, { text: 'commit + sha256 per file', size: 16 }, { text: 'dataset.lock.json: the same pin', size: 16 }], step: 'step-01' },
    { id: 'ver', kind: 'gate', x: 700, y: 190, w: 280, h: 90, lines: ['One number', { text: 'MARKETING_VERSION == vendored', size: 16 }, { text: 'APP_VERSION · BUILD=N rises', size: 16 }], step: 'step-02' },
    { id: 'stage', kind: 'store', x: 1010, y: 190, w: 260, h: 90, lines: ['Stage the DB pair', { text: 'build_ios_db.py · public', size: 16 }, { text: 'sqlite + manifest in build/', size: 16 }], step: 'step-03' },
    { id: 'lic', kind: 'gate', x: 1300, y: 190, w: 240, h: 90, lines: ['Licence gate', { text: 'en needs LICENSED: true', size: 16 }, { text: 'scripture sha certified', size: 16 }], step: 'step-04' },
    { id: 'xcode', kind: 'box', x: 60, y: 330, w: 340, h: 100, lines: ['XcodeGen + archive', { text: 'temporary spec: 3 lines differ', size: 16 }, { text: 'Release · Team ID · build number', size: 16 }, { text: 'Xcode ≥ the App Store floor', size: 16 }], step: 'step-05' },
    { id: 'upload', kind: 'ext', x: 430, y: 330, w: 320, h: 100, lines: ['Export → App Store Connect', { text: 'UPLOAD=1: the export is the upload', size: 16 }, { text: 'CHANNEL=appstore declared up front', size: 16 }], step: 'step-06' },
    { id: 'prove', kind: 'good', x: 780, y: 330, w: 340, h: 100, lines: ['Prove what was built', { text: 'Info.plist version + build', size: 16 }, { text: 'the manifest inside the .app', size: 16 }, { text: 'equals what was staged', size: 16 }], step: 'step-06' },
    { id: 'ledger', kind: 'pin', x: 1150, y: 330, w: 390, h: 100, lines: [{ text: 'ios/testflight-builds.json', mono: true }, { text: 'version · build · source + platform +', size: 16 }, { text: 'dataset commit · profile · channel · sdk', size: 16 }], step: 'step-07' },
    { id: 'complete', kind: 'good', x: 60, y: 480, w: 1480, h: 90, lines: ['Complete: check_release_complete.py X.Y.Z', { text: 'production web and API report X.Y.Z from the tag’s commit, and the ledger has an appstore build of X.Y.Z with that platform_commit', size: 16 }], step: 'step-07' },
    { id: 'launch', kind: 'box', x: 60, y: 690, w: 340, h: 100, lines: ['LaunchIntegrity.run', { text: 'SHA-256 of the bundled database', size: 16 }, { text: 'vs manifest db_sha256', size: 16 }], step: 'step-08' },
    { id: 'cache', kind: 'note', x: 430, y: 690, w: 350, h: 100, lines: ['Fingerprint cache', { text: 'after a full verify: sha · size · time', size: 16 }, { text: '· bundle + manifest; any change →', size: 16 }, { text: 'full re-verify; failures never cached', size: 16 }], step: 'step-08' },
    { id: 'ok', kind: 'good', x: 60, y: 830, w: 340, h: 110, lines: ['Bit-identical to the corpus', { text: 'every invariant holds by construction', size: 16 }, { text: 'cheap structural checks add depth', size: 16 }], step: 'step-08' },
    { id: 'refuse', kind: 'gate', x: 430, y: 830, w: 350, h: 110, lines: ['Any failure → fail closed', { text: 'the app refuses to present scripture', size: 16 }, { text: '"Scripture integrity check failed"', size: 16 }, { text: 'report under More → About → Integrity', size: 16 }], step: 'step-08' },
    { id: 'saved', kind: 'store', x: 850, y: 690, w: 690, h: 100, lines: ['A separate, writable SwiftData store', { text: 'SavedLine: verbatim Gurmukhi, no saroop markup · never the read-only corpus', size: 16 }, { text: 'SavedLineSchemaV1 · a migration plan for what comes next', size: 16 }], step: 'step-09' },
    { id: 'ladder', kind: 'note', x: 850, y: 830, w: 690, h: 110, lines: ['The store ladder', { text: 'open with the versioned schema → retry →', size: 16 }, { text: 'destroy and recreate only on a second consecutive failure', size: 16 }, { text: 'Nitnem progress: its own JSON in the App Group, schema v1 frozen', size: 16 }], step: 'step-09' },
  ],
  edges: [
    { from: 'tag', to: 'vendor', step: 'step-01' }, { from: 'vendor', to: 'ver', step: 'step-02' }, { from: 'ver', to: 'stage', step: 'step-03' }, { from: 'stage', to: 'lic', step: 'step-04' },
    { from: 'lic', to: 'xcode', step: 'step-05', via: [[1420, 305], [230, 305]] },
    { from: 'xcode', to: 'upload', step: 'step-06' }, { from: 'upload', to: 'prove', step: 'step-06' }, { from: 'prove', to: 'ledger', step: 'step-07' },
    { from: 'ledger', to: 'complete', dashed: true, step: 'step-07' },
    { from: 'launch', to: 'cache', dashed: true, step: 'step-08' }, { from: 'launch', to: 'ok', label: 'match', step: 'step-08', dx: 60 }, { from: 'launch', to: 'refuse', label: 'mismatch', step: 'step-08' },
    { from: 'saved', to: 'ladder', step: 'step-09' },
  ],
  steps: [
    { id: 'step-01', title: 'Vendor the release', caption: 'make vendor-sync-platform REF=vX.Y.Z copies the platform’s golden contract and contributors roster into the app repository at their upstream paths and records the commit, a sha256 per file and the platform’s APP_VERSION in vendor.lock.json. The dataset is the same sggs-data pin the platform serves; the iOS database builder is vendored from it. CI proves every vendored file equals its lock entry and its source.', link: '/ios/how-the-app-takes-a-release/' },
    { id: 'step-02', title: 'One number', caption: 'MARKETING_VERSION must equal the vendored platform version; CURRENT_PROJECT_VERSION stays 1 and every upload passes BUILD=N, kept monotonic within a marketing version by the ledger before any archive starts. A release is re-archived at the same number even for a web-only change.', link: '/engineering/delivery/' },
    { id: 'step-03', title: 'Stage the database pair', caption: 'From a clean, detached worktree at the tag, make testflight derives the database for the chosen profile — public, Gurmukhi-only, by default — into a staging directory under ios/App/build, with its manifest: profile, db_sha256, scripture_sha256, en_bundled, size, tables. The developer’s own pair in ios/Resources is never read or written.', link: '/ios/db-pair-and-launch-integrity/' },
    { id: 'step-04', title: 'The licence gate', caption: 'check_release_license.sh runs on that exact artifact: English may be bundled only with LICENSED: true in the attestation; scripture_sha256 must equal the certified corpus value; the artifact must hash to its manifest; every golden suite must be present; and on the App Store channel the Nitnem non-SGGS text must carry the scholar’s REVIEWED: true.', link: '/ios/db-pair-and-launch-integrity/' },
    { id: 'step-05', title: 'XcodeGen and archive', caption: 'A temporary sibling spec differs from project.yml in exactly three lines (the name and the two database resource paths, pointed at the staging directory); XcodeGen generates a throw-away project; xcodebuild archives the Release configuration with the Team ID and build number from the command line under automatic signing. Xcode must be at or above Apple’s SDK floor.', link: '/ios/db-pair-and-launch-integrity/' },
    { id: 'step-06', title: 'Upload and prove', caption: 'The archive is exported for App Store Connect; with UPLOAD=1 the export is the upload. Then the script proves what was built: the Info.plist version and build and the manifest inside the .app equal what was staged. CHANNEL=appstore is declared up front, because a TestFlight binary and an App Store binary are the same upload.', link: '/ios/how-the-app-takes-a-release/' },
    { id: 'step-07', title: 'The ledger, and “complete”', caption: 'The build is appended to ios/testflight-builds.json — version, build, source_commit, platform_commit, dataset_commit, profile, channel, db_sha256, Xcode and SDK — and committed with the release; make appstore-preflight submits only a build recorded on the App Store channel. scripts/release/check_release_complete.py X.Y.Z then proves production’s web and API report X.Y.Z from the tag’s commit and that the ledger has an appstore build of X.Y.Z with that platform commit.', link: '/ios/how-the-app-takes-a-release/' },
    { id: 'step-08', title: 'On the device: launch integrity', caption: 'Before any scripture is shown, LaunchIntegrity hashes the bundled database and compares it with the manifest’s db_sha256; a match means the database is bit-identical to the proven corpus. After one full streaming verify a fingerprint (sha, file size and mtime, bundle version, manifest sha) lets later launches skip the re-hash — but any change forces a full re-verify and a failure is never cached. On any failure the app refuses to present scripture and shows the report under About → Integrity. The threat model is accidents; anti-tamper is code signing’s job.', link: '/ios/db-pair-and-launch-integrity/' },
    { id: 'step-09', title: 'On the device: the bookmarks store', caption: 'Saved verses live in a separate writable SwiftData store — never the read-only corpus — holding verbatim Gurmukhi without saroop markup, under a versioned schema (SavedLineSchemaV1) with a migration plan. Opening it climbs a ladder: the versioned schema, a retry, and only on a second consecutive failure destroy-and-recreate, so a transient error never costs a reader their bookmarks. Nitnem progress is its own JSON file in the App Group with a frozen v1 schema.', link: '/ios/nitnem-for-engineers/' },
  ],
};
