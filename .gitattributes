db/sggs.sqlite filter=lfs diff=lfs merge=lfs -text
*.sqlite filter=lfs diff=lfs merge=lfs -text

# Scripture and contract bytes are never rewritten by git (no EOL conversion, ever).
corpus/** -text
contract/*.ndjson -text
